<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginStart="16dp"
    android:layout_marginTop="8dp"
    android:layout_marginEnd="16dp"
    android:background="?attr/rdCardBg"
    android:orientation="vertical"
    android:padding="13dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center_vertical"
        android:orientation="horizontal">

        <TextView
            android:id="@+id/tvVocabWord"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:ellipsize="end"
            android:maxLines="1"
            android:text="word"
            android:textAppearance="?attr/rdTitleAppearance"
            android:textSize="17sp" />

        <TextView
            android:id="@+id/tvVocabCount"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginEnd="6dp"
            android:text=""
            android:textAppearance="?attr/rdSubtitleAppearance"
            android:textColor="?attr/rdAccent" />

        <ImageButton
            android:id="@+id/btnVocabSpeak"
            android:layout_width="34dp"
            android:layout_height="34dp"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:contentDescription="Say this word"
            android:src="@android:drawable/ic_lock_silent_mode_off"
            app:tint="?attr/rdTextSecondary" />

        <ImageButton
            android:id="@+id/btnVocabDelete"
            android:layout_width="34dp"
            android:layout_height="34dp"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:contentDescription="Remove this word"
            android:src="@android:drawable/ic_menu_delete"
            app:tint="?attr/rdTextTertiary" />
    </LinearLayout>

    <TextView
        android:id="@+id/tvVocabGloss"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="4dp"
        android:ellipsize="end"
        android:maxLines="4"
        android:text="definition"
        android:textAppearance="?attr/rdBodyAppearance" />

    <!-- Where you met the word: the part that actually makes it stick. -->
    <TextView
        android:id="@+id/tvVocabSentence"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="7dp"
        android:ellipsize="end"
        android:maxLines="3"
        android:text=""
        android:textAppearance="?attr/rdSubtitleAppearance"
        android:textStyle="italic" />

    <TextView
        android:id="@+id/tvVocabMeta"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="6dp"
        android:ellipsize="end"
        android:maxLines="1"
        android:text=""
        android:textAppearance="?attr/rdSubtitleAppearance"
        android:textColor="?attr/rdTextTertiary" />
</LinearLayout>
